# >>> aikata managed >>>
# --- シークレット ---
.env
.env.local

# --- aikata 作業ファイル ---
# 既存ディレクトリに対する `aikata init` の提案物（一時的）。
/.aikata-proposed/

# --- AI ツールが生成する成果物 ---
# 規範 AGENTS.md から `aikata generate` で再生成可能。生成物をコミット
# したい場合は以下の行を削除する。
CLAUDE.md
.cursor/rules/
.github/copilot-instructions.md
GEMINI.md
.windsurfrules
# <<< aikata managed <<<
