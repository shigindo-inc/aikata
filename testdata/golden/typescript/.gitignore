# --- Secrets ---
.env
.env.local

# --- aikata working files ---
# Proposals from `aikata init` against existing directories (transient).
/.aikata-proposed/

# --- AI tool-generated artifacts ---
# Regenerable from canonical AGENTS.md via `aikata generate`. Remove
# these lines if you would rather commit the generated files.
CLAUDE.md
.cursor/rules/
.github/copilot-instructions.md
GEMINI.md
.windsurfrules
