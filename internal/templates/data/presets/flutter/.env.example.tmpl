# Environment variables for {{.ProjectName}}.
#
# Copy this file to `.env` (which is gitignored) and fill in real values.
# Flutter apps typically load env vars at build time via
# `--dart-define=KEY=value` rather than at runtime; document each
# variable here so the `aikata doctor` check can verify cross-references
# in `AGENTS.md` or `ARCHITECTURE.md`.

# Example:
# API_BASE_URL=https://api.{{kebab .ProjectName}}.example
# SENTRY_DSN=
