# {{.ProjectName}} の環境変数。
#
# 本ファイルを `.env`（gitignore 済み）にコピーして実際の値を入れる。
# 各変数はここに記述する — `aikata doctor` が `.env.example` の各エントリが
# `AGENTS.md` または `ARCHITECTURE.md` で言及されているかを検証する。

# 例:
# DATABASE_URL=postgres://user:pass@localhost:5432/{{kebab .ProjectName}}
# NODE_ENV=development
# LOG_LEVEL=info
