interface:
  display_name: "manage-docs (aikata CLI)"
  short_description: "Scaffold and audit AI-readable project docs via the aikata CLI"
  default_prompt: "Use $manage-docs to scaffold or audit this project's AI-readable documentation."
policy:
  allow_implicit_invocation: true
