---
description: Run an aikata CLI lifecycle operation (init, fill, generate, doctor, sync, enable, new) on this project's AI-readable docs
---

The user invoked the slash command `/aikata:manage-docs`.

Immediately invoke the `aikata:manage-docs` skill with the Skill tool and
follow its instructions. No preamble or confirmation is needed — the
skill's guidance takes priority.

Arguments the user passed: $ARGUMENTS
