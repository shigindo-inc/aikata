---
description: Bring this repo's docs up to the latest aikata — update the binary, sync templates, fill gaps, run doctor, retire deprecated docs, regenerate
---

The user invoked the slash command `/aikata:refresh-docs`.

Immediately invoke the `aikata:refresh-docs` skill with the Skill tool and
follow its instructions. No preamble or confirmation is needed — the
skill's guidance takes priority. Confirm with the user before any
destructive step (binary upgrade, deleting deprecated docs), as the skill
directs.

Arguments the user passed: $ARGUMENTS
